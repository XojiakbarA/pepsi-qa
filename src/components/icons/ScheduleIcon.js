import scheduleIcon from '../../assets/icons/schedule.png'

const ScheduleIcon = ({ width = 20 }) => {

    return (
        <img src={scheduleIcon} width={width} alt="barometer-icon"/>
    )
}

export default ScheduleIcon