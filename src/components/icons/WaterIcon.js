import waterIcon from '../../assets/icons/water.png'

const WaterIcon = ({ width = 20 }) => {

    return (
        <img src={waterIcon} width={width} alt="bottle-icon"/>
    )
}

export default WaterIcon